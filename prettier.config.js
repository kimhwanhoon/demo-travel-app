const config = {
  singleAttributePerLine: true,
  jsxSingleQuote: true,
  singleQuote: true,
  bracketSpacing: true,
  arrowParens: 'avoid',
  plugins: ['prettier-plugin-tailwindcss'],
};

module.exports = config;
